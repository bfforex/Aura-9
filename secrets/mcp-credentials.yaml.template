# MCP Server Credentials — copy to mcp-credentials.yaml and fill in values
# NEVER commit the filled file to git
servers:
  example_server:
    url: "https://example-mcp-server.com/rpc"
    api_key: "YOUR_API_KEY_HERE"
    tier: "auto"          # auto | auto_with_log | human_gate
    daily_limit: 100
